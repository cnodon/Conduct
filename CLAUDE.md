# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概述

Conduct 是一个用于扫描和分析 Claude Code Skills 配置状态的工具。它可以检查全局 skills 和项目级 skills 的配置。

## 当前项目状态

这是一个新初始化的项目,目前只包含 Claude Code 的基础配置:
- `.claude/settings.local.json` - 本地权限配置

## 预期功能

基于项目描述,该工具应该能够:
1. 扫描全局 Claude Code skills 配置(通常位于 `~/.claude/skills/`)
2. 扫描项目内的 skills 配置(位于项目的 `.claude/skills/` 或类似目录)
3. 扫描嵌套目录的 skills 配置(如 `packages/*/. claude/skills/`)
4. 分析 skills 的配置状态,包括启用/禁用状态、配置完整性等
5. 生成配置报告或诊断信息
6. 提供 GUI 界面用于可视化管理 Skills

## 开发指南

### 项目结构

```
conduct/
├── src/
│   ├── core/              # 核心引擎
│   │   ├── LocationResolver.ts
│   │   ├── Scanner.ts
│   │   ├── Parser.ts
│   │   ├── Validator.ts
│   │   └── Reporter.ts
│   ├── cli/               # CLI 模式
│   │   └── index.ts
│   ├── gui/               # GUI 模式 (Electron)
│   │   ├── main/          # Electron 主进程
│   │   │   ├── index.ts
│   │   │   └── ipc.ts
│   │   └── renderer/      # React 前端
│   │       ├── components/
│   │       ├── hooks/
│   │       └── App.tsx
│   └── types/             # TypeScript 类型定义
├── package.json
└── tsconfig.json
```

### Claude Code Skills 位置

Claude Code 的 skills 通常存储在以下位置:
- **全局 skills**: `~/.claude/skills/` 或通过 Claude Code 配置指定的目录
- **项目 skills**: `<项目根目录>/.claude/skills/`
- **嵌套 skills**: `<项目根目录>/packages/*/.claude/skills/`
- **Skills 配置**: `.claude/settings.json` 或 `.claude/settings.local.json`

### 实现建议

#### CLI 模式

扫描 skills 配置时需要:
1. 读取 `.claude/settings.json` 和 `.claude/settings.local.json` 文件
2. 解析 skills 路径配置
3. 遍历 skills 目录,读取每个 skill 的元数据(通常在 YAML frontmatter 中)
4. 检查 skill 文件的有效性(格式、必需字段等)
5. 生成结构化的配置状态报告

#### GUI 模式

GUI 界面应该提供:
1. **分层级展示**
   - 个人全局 Skills 独立区域显示
   - 项目级 Skills 独立区域显示
   - 项目级 Skills 下可展开查看嵌套目录的 Skills

2. **状态可视化**
   - ✅ 配置正确的 Skills
   - ⚠️ 有警告的 Skills
   - ❌ 配置错误的 Skills
   - 🔄 被高优先级覆盖的 Skills

3. **交互功能**
   - 点击 Skill 查看详细信息(元数据、YAML 配置、验证结果)
   - 展开/折叠嵌套目录树
   - 实时监听文件变更并刷新
   - 一键打开文件编辑或在文件管理器中显示

### 技术要点

1. **YAML 解析**: 使用 `js-yaml` 解析 SKILL.md 的 frontmatter
2. **文件监听**: 使用 `chokidar` 监听 skills 目录变更
3. **IPC 通信**: Electron 主进程和渲染进程通过 IPC 通信
4. **树形数据结构**: 将扫描结果转换为层级树形数据
5. **实时验证**: 支持实时重新验证 Skill 配置
